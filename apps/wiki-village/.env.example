# Server-side only. Never use VITE_ variables for these values.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra
OPENAI_REASONING_EFFORT=low
# The architect uses the same server-only credentials and reports not-configured/unavailable/malformed-response safely.
# Do not expose these as VITE_ variables. Live smoke calls require KNOWLEDGE_GUILD_PROVIDER_SMOKE=1.
