# Server-side only. Never use VITE_ variables for these values.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra
OPENAI_REASONING_EFFORT=low
# The architect uses the same server-only credentials. Do not expose this as VITE_ variables.
