import { lstat, mkdir, readFile, readdir, realpath, writeFile } from 'node:fs/promises'
import { execFile } from 'node:child_process'
import { createHash } from 'node:crypto'
import { dirname, join, relative, resolve, sep } from 'node:path'
import { fileURLToPath } from 'node:url'
import { activityBand, inferGuildRole } from '../src/guild.js'
import { buildFlowSummary, isValidFlowSummary, publicSource } from '../src/flow-summary.js'

const appRoot = resolve(fileURLToPath(new URL('..', import.meta.url))); const repoRoot = resolve(process.argv[2] || process.env.WIKI_VILLAGE_REPO_ROOT || resolve(appRoot, '..', '..')); const output = join(appRoot, 'src', 'data', 'wiki-snapshot.json'); const evidenceOutput = join(appRoot, 'server', 'wiki-evidence.json'); const membersRoot = join(repoRoot, 'members')
const safeId = /^[a-z0-9-]+$/; const ignored = new Set(['raw', 'output', 'private', 'secrets', '.obsidian', 'node_modules']); const types = new Set(['fact', 'personal-opinion', 'hypothesis', 'wiki-record']); const digest = value => createHash('sha256').update(value).digest('hex'); const within = (base, candidate) => candidate === base || candidate.startsWith(`${base}${sep}`); const source = path => relative(repoRoot, path).replaceAll('\\', '/')
const front = text => text.match(/^---\s*\n([\s\S]*?)\n---/)?.[1] || ''; const field = (text, key) => text.match(new RegExp(`^${key}:\\s*(.+)$`, 'm'))?.[1]?.replace(/^['"]|['"]$/g, ''); const title = (text, fallback) => field(front(text), 'title') || text.match(/^#\s+(.+)$/m)?.[1] || fallback; const excerpt = text => text.replace(/^---[\s\S]*?---\s*/m, '').replace(/```[\s\S]*?```/g, '').replace(/[#>*`|]/g, ' ').replace(/\s+/g, ' ').trim().slice(0, 230); const knowledgeType = text => { const value = field(front(text), 'knowledgeType'); return types.has(value) ? value : 'wiki-record' }
const recordId = (scope, absolute) => `${scope}-${source(absolute).replace(/[^a-z0-9]+/gi, '-').replace(/-+$/, '')}`; const manifestFor = records => ({ version: 1, recordCount: records.length, contentDigest: digest(JSON.stringify(records.map(record => ({ id: record.id, scope: record.scope, memberId: record.memberId || null, source: record.source, title: record.title, knowledgeType: record.knowledgeType, contentDigest: record.contentDigest })).sort((a, b) => a.id.localeCompare(b.id)))) })
const safeDocument = document => document && publicSource(document.source) && (document.scope === 'project' || document.scope === 'personal' && safeId.test(document.memberId || '') && document.source.startsWith(`members/${document.memberId}/wiki/`))
const validArtifact = (snapshot, evidence) => snapshot && evidence && Array.isArray(snapshot.documents) && Array.isArray(evidence.documents) && isValidFlowSummary(snapshot.flow) && snapshot.flow.evidencePaths.every(path => snapshot.documents.some(document => document.source === path)) && JSON.stringify(snapshot.manifest) === JSON.stringify(evidence.manifest) && JSON.stringify(snapshot.manifest) === JSON.stringify(manifestFor(snapshot.documents)) && JSON.stringify(evidence.manifest) === JSON.stringify(manifestFor(evidence.documents)) && new Set(snapshot.documents.map(item => item.id)).size === snapshot.documents.length && new Set(snapshot.documents.map(item => item.source)).size === snapshot.documents.length && snapshot.documents.length === evidence.documents.length && snapshot.documents.every(item => { const record = evidence.documents.find(candidate => candidate.id === item.id); return safeDocument(item) && record && record.scope === item.scope && record.memberId === item.memberId && record.source === item.source && record.title === item.title && record.knowledgeType === item.knowledgeType && record.contentDigest === item.contentDigest && /^[a-f0-9]{64}$/.test(item.contentDigest || '') && digest(record.body || '') === record.contentDigest })
async function preserve(reason) { const [savedSnapshot, savedEvidence] = await Promise.all([readFile(output, 'utf8').then(JSON.parse), readFile(evidenceOutput, 'utf8').then(JSON.parse)]).catch(() => [null, null]); if (!validArtifact(savedSnapshot, savedEvidence)) throw new Error(`Wiki sources are unavailable (${reason}); refusing to publish invalid artifacts.`); console.log(`Preserved committed Wiki snapshot (${reason}).`) }
async function load(file) { const absolute = await realpath(file); if (!within(repoRoot, absolute)) return null; return { absolute, text: await readFile(absolute, 'utf8') } }
async function walk(directory, out = []) { let root; try { root = await realpath(directory) } catch { return out }; for (const entry of (await readdir(directory, { withFileTypes: true })).sort((a, b) => a.name.localeCompare(b.name))) { if (entry.name.startsWith('.') || ignored.has(entry.name.toLowerCase())) continue; const absolute = join(directory, entry.name); const stat = await lstat(absolute); if (stat.isSymbolicLink()) continue; if (stat.isDirectory()) await walk(absolute, out); if (stat.isFile() && entry.name.endsWith('.md') && !entry.name.toLowerCase().endsWith('.private.md')) { const resolved = await realpath(absolute); if (within(root, resolved)) out.push(resolved) } }; return out }
const toDocument = (file, scope, memberId = null) => ({ id: recordId(memberId ? `member-${memberId}` : scope, file.absolute), scope, memberId, author: memberId || 'project', category: scope === 'project' ? '프로젝트 Wiki' : '개인 Wiki', source: source(file.absolute), title: title(file.text, memberId || 'Project Wiki'), updated: field(front(file.text), 'updated') || null, status: 'recorded', knowledgeType: knowledgeType(file.text), contentDigest: digest(file.text), excerpt: excerpt(file.text), body: file.text })
async function activity(memberId) { try { const date = (await new Promise((resolvePromise, reject) => execFile('git', ['log', '-1', '--format=%cI', '--', `members/${memberId}/wiki`], { cwd: repoRoot }, (error, stdout) => error ? reject(error) : resolvePromise(String(stdout).trim())))).slice(0, 10); return activityBand(/^\d{4}-\d{2}-\d{2}$/.test(date) ? date : null) } catch { return activityBand(null) } }
async function member(memberId) { const files = (await Promise.all((await walk(join(membersRoot, memberId, 'wiki'))).map(file => load(file)))).filter(Boolean); if (!files.length) return null; const documents = files.map(file => toDocument(file, 'personal', memberId)); return { id: memberId, memberId, displayName: memberId, documentIds: documents.map(item => item.id), documentCount: documents.length, role: inferGuildRole(documents), activity: await activity(memberId), documents } }

const sourceMissing = process.env.WIKI_VILLAGE_SIMULATE_SOURCE_MISSING === '1'; if (process.env.VERCEL === '1' || sourceMissing) { await preserve('source root missing'); process.exit(0) }
const projectFiles = (await Promise.all((await walk(join(repoRoot, 'projects', 'wiki'))).map(file => load(file)))).filter(Boolean)
let entries = []; try { entries = (await readdir(membersRoot, { withFileTypes: true })).filter(entry => entry.isDirectory() && safeId.test(entry.name)).sort((a, b) => a.name.localeCompare(b.name)) } catch {}
const members = (await Promise.all(entries.map(entry => member(entry.name)))).filter(Boolean); const projectDocuments = projectFiles.map(file => toDocument(file, 'project')); const evidenceDocuments = [...projectDocuments, ...members.flatMap(item => item.documents)]; const documents = evidenceDocuments.map(({ body, ...document }) => document); const flow = buildFlowSummary(evidenceDocuments)
const manifest = manifestFor(documents); const state = flow.status === 'observed' ? 'FLOW_READY' : documents.length ? 'FLOW_INSUFFICIENT' : 'FLOW_UNINITIALIZED'; const snapshot = { version: 7, projectState: state, generatedAt: flow.lastUpdatedAt, boundary: 'WIKI SNAPSHOT · projects/wiki and members/<id>/wiki only · no context/raw/output/private/secrets', decisionState: 'Official decisions exist only in decisions/ after explicit approval; flow is an observation, not a decision.', manifest, flow, members: members.map(({ documents: memberDocuments, ...item }) => item), documents }
await mkdir(dirname(output), { recursive: true }); await writeFile(output, `${JSON.stringify(snapshot, null, 2)}\n`); await writeFile(evidenceOutput, `${JSON.stringify({ version: 6, manifest, documents: evidenceDocuments }, null, 2)}\n`); console.log(`Wrote ${relative(appRoot, output)} for ${state} with ${members.length} members.`)
