# Members

Create one directory per member using a lowercase `member-id` made from letters, numbers, and hyphens. The committed `example-member` is synthetic fixture data only. Do not add another person's private material, raw inputs, output, local settings, or secrets.
